# Spec 0002 — Progress

Progress for the MVP work tracked against `0001-mvp.md` and `0002-mvp-user-scenarios.md` lives in **`0001-mvp-progress.md`** in this same directory. That file predates this one and is the single source of truth for phase status, round logs, and reviewer scores.

This file exists only as a pointer so any tooling that looks for `0002-progress.md` is redirected without splitting state.

→ See `docs/spec/0001-mvp-progress.md`.
